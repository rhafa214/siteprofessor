# Identidade do Projeto

## Nomes Encontrados
- **EduAssistente**: Aparece em `index.html` (title), `src/views/LoginView.tsx`, `src/views/AddonSidebar.tsx`. Este parece ser o nome oficial adotado na interface.
- **react-example**: Nome técnico no `package.json` (`"name": "react-example"`). Isso é um resquício provável de um template padrão.
- **eduChatCurrent**, **eduPlan**, **eduCurrentChatId**, **eduChatHistory**: Prefixos internos no localStorage. A raiz nominal é `edu`.

## Conflitos/Divergências
O `package.json` precisa ser atualizado, assim como metadados do projeto, se o nome final for EduAssistente. Por hora a UI apresenta consistência com "EduAssistente".
